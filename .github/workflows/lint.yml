name: Lint Rules
on:
  pull_request:
    paths: ['src/**', 'scripts/**']
  push:
    branches: [master]
    paths: ['src/**', 'scripts/**']

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run lint rules
        run: bash scripts/lint-rules.sh
