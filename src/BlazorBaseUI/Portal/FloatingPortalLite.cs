namespace BlazorBaseUI.Portal;

/// <summary>
/// A lightweight portal element that moves floating content to a different part of the DOM
/// without focus management overhead. Used by components that don't need tabbable logic
/// (e.g., Tooltip).
/// Renders a <c>&lt;div&gt;</c> element.
/// </summary>
public partial class FloatingPortalLite;
