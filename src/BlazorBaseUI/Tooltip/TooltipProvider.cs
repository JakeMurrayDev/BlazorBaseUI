namespace BlazorBaseUI.Tooltip;

/// <summary>
/// Provides shared configuration for a group of tooltip components.
/// Does not render an HTML element.
/// </summary>
public sealed partial class TooltipProvider;
